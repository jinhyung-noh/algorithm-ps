class Solution:
    def trap(self, height: list[int]) -> int:
        stack = []

        for i in range(len(height)):
            while "bigger than before stack":
                pass


            stack.append(i)


        return None