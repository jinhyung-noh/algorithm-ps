function solution(n) {
    const arr = [];
    while (n > 0) {
        arr.push(n % 3);
        n = parseInt(n / 3);
    }
    let answer = 0;
    for (let i = 0; i < arr.length; i++) {

    }
    return;
}

solution(45);